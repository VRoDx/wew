version = "1.0.0"
description = "Long press a sticker in the sticker panel to add it to favorites"

aliucord {
    changelog.set(
        """
        # 1.0.0
        * Initial release — long press sticker to favorite
        """.trimIndent(),
    )
    deploy.set(true)
}
