version = "1.0.0"
description = "Suggests stickers from your servers when you type :: followed by text"

aliucord {
    changelog.set(
        """
        # 1.0.0
        * Initial release — sticker suggestions with :: trigger
        """.trimIndent(),
    )
    deploy.set(true)
}
